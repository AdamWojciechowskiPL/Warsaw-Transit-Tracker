# ============================================================
# Natywna integracja Netlify + Auth0 Extension
# ============================================================
# Zmienne poniżej są AUTOMATYCZNIE wstrzykiwane przez Netlify
# Extension dla Auth0 (nie musisz ich ręcznie ustawiać w panelu).
#
# Jeśli konfigurujesz lokalnie (netlify dev), skopiuj wartości
# z panelu Netlify: Site config > Environment variables
# i wklej do .env.local
# ============================================================

# --- Backend (Netlify Functions) --- wstrzykiwane automatycznie
AUTH0_DOMAIN=your-tenant.eu.auth0.com
AUTH0_ISSUER_BASE_URL=https://your-tenant.eu.auth0.com
AUTH0_CLIENT_ID=your_client_id
AUTH0_AUDIENCE=https://transit-tracker-api

# --- Frontend (Vite build) ---
# W panelu Netlify Extension ustaw prefix: "VITE_"
# Extension wygeneruje automatycznie:
VITE_AUTH0_DOMAIN=your-tenant.eu.auth0.com
VITE_AUTH0_CLIENT_ID=your_client_id
VITE_AUTH0_AUDIENCE=https://transit-tracker-api

# --- Neon PostgreSQL ---
NETLIFY_DATABASE_URL=postgresql://user:password@host/dbname
